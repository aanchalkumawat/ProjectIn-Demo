const Teacher = require("./Teacher");
const Coordinator = require("./Coordinator");
const Panel = require("./Panel");
const User = require("./User"); // Assuming User model exists

module.exports = { Teacher, Coordinator, Panel, User };
